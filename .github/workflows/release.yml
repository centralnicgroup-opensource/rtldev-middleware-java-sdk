name: Release
on:
  push:
    branches:
      - master

jobs:
  release:
    uses: centralnicgroup-opensource/rtldev-middleware-shareable-workflows/.github/workflows/java-sdk-release.yml@RSRMID-2020-java-sdk-for-cnr
    secrets: inherit
